Для комфортной разработки поставьте virtual-venv:
```sh
python -m venv venv/
# внутри venv
pip install -r requirements.txt
```
чтобы запустить это, вам нужен Docker Desktop и если он есть, то:
```sh
docker compose up
```
